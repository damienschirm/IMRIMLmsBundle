{% extends "IMRIMLmsBundle::layout.html.twig" %}

{% block content %} 

<h1>Mon profil</h1>

<form action="{{ path('user_profile') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
        <p>
            <button type="submit">Editer</button>
        </p>
    </form>
{% endblock %}

{% block menu %} 
    <a href="{% if is_granted('ROLE_ADMIN')%}{{ path('welcome_admin') }} {% else %}{{ path('imrim_lms_course_list') }} {% endif %}">Accueil</a>
{% endblock %}
